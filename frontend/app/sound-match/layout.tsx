import GameLayout from "@/components/game-layout";

export default function SoundMatchLayout({ children }: { children: React.ReactNode }) {
  return <GameLayout title="sound match">{children}</GameLayout>;
}
