import GameLayout from "@/components/game-layout";

export default function WordBuilderLayout({ children }: { children: React.ReactNode }) {
  return <GameLayout title="word builder">{children}</GameLayout>;
}
