import GameLayout from "@/components/game-layout";

export default function RapidMatchLayout({ children }: { children: React.ReactNode }) {
  return <GameLayout title="rapid match">{children}</GameLayout>;
}
